<!DOCTYPE html>
<html lang="en">
    <head>
        <title> Mini Whatsapp</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="/css/style.css">

    </head>
    <body>

        <div class="row mt-3">
            <div class="col-8 offset-2">
            <h3>Edit the Listing</h3>
            <p class="text-center">
                This chat was sent from <b><%= cchat.from %></b> to <b><%= cchat.to %></b>
            </p>

            <form method="POST" action="/chats/<%= cchat._id %>?_method=PUT" class="needs-validation" novalidate>
                <div class="mb-3">
                    <label for="msz" class="form-label">Message</label>
                    <input name="chat[msz]" value="<%= cchat.msz %>" type="text" class="form-control" required>
                    <div class="invalid-feedback">Please enter valid message</div>
                </div>
                <button class="btn btn-success edit-btn mt-3 mb-3">Edit</button>
            </form>

        </div>
   </body>     
</html>